# ========================================
# MINIMAL REQUIREMENTS FOR WEAK MACHINES
# ========================================
# Use this file if the full requirements.txt
# causes memory issues during installation
#
# To install: pip install -r requirements_minimal.txt
# ========================================

# Core Framework (Essential)
Flask==2.3.3
Werkzeug==2.3.7

# Database (Essential)
Flask-SQLAlchemy==3.0.5
SQLAlchemy==2.0.21

# Report Generation (Essential)
reportlab==4.0.4

# Utilities (Essential)
python-dateutil==2.8.2

# Email Validation (Essential)
email-validator==2.0.0

# ========================================
# OPTIONAL PACKAGES (Install later if needed)
# ========================================
# APScheduler==3.10.4    # Auto-reports (can disable)
# pywin32>=306           # PDF printing (Windows only)
#
# To install optional packages:
# pip install APScheduler==3.10.4
# pip install pywin32>=306
# ========================================

